# 依赖与构建产物
node_modules/
lib/
dist/
*.tsbuildinfo

# 认知运行时数据（双库 + 制品索引等），不跟踪不提交
.omb/

# 嵌入权重：BGE-small-zh-v1.5 约 23GB（含 .onnx_data），绝不入库
# 用 scripts/fetch-embedding-model.ts 获取
models/

# 日志与临时文件
*.log
Thumbs.db
.DS_Store

# 环境与密钥
.env
.env.*
!.env.example
lib-gen/
