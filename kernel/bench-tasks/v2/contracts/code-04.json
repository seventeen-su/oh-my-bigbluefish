{
  "id": "code-04",
  "category": "code",
  "requirement": "用 language 指定的语言实现函数 deepClone(obj)：深拷贝任意 JSON 兼容对象（数组/嵌套对象/基本类型），不改动原对象。然后静态推演 test-cases 中的每个用例（输入 → 期望输出），输出 { passed, failed, total } 摘要（passed + failed = total）。",
  "input_artifacts": [
    {
      "name": "test-cases",
      "description": "deepClone 的断言用例：{ name, input, expected }；expected 为与 input 深度相等的克隆值",
      "kind": "test-cases",
      "content": [
        { "name": "primitive number", "input": 42, "expected": 42 },
        { "name": "string", "input": "hello", "expected": "hello" },
        {
          "name": "nested object",
          "input": { "a": 1, "b": { "c": [1, 2, { "d": 3 }] } },
          "expected": { "a": 1, "b": { "c": [1, 2, { "d": 3 }] } }
        },
        { "name": "array", "input": [1, [2, [3]]], "expected": [1, [2, [3]]] },
        { "name": "null", "input": null, "expected": null }
      ]
    },
    {
      "name": "language",
      "description": "实现语言",
      "kind": "text",
      "content": "TypeScript"
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "passed": { "type": "number" },
      "failed": { "type": "number" },
      "total": { "type": "number" }
    },
    "required": ["passed", "failed", "total"]
  },
  "verifier": { "kind": "tests" },
  "generator": { "name": "code-04 reference", "version": "1.0.0" }
}
