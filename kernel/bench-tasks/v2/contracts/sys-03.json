{
  "id": "sys-03",
  "category": "sys",
  "requirement": "按变更规格删除文件：输出前后状态断言 { before, after }——目标路径从 exists 变为 absent。",
  "input_artifacts": [
    {
      "name": "changes",
      "description": "文件删除变更规格：operation=delete / path 目标文件路径",
      "kind": "json",
      "content": {
        "operation": "delete",
        "path": "tmp/x.log"
      }
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "before": { "type": "object" },
      "after": { "type": "object" }
    },
    "required": ["before", "after"]
  },
  "verifier": { "kind": "state_assert" },
  "generator": { "name": "sys-03 reference", "version": "1.0.0" }
}
