{
  "id": "research-03",
  "category": "research",
  "requirement": "根据 topic 与 rubric 撰写规范答案并输出 { answer: string }；答案必须覆盖 rubric.required_terms 中的全部术语（离线规则化 judge 判定）。",
  "input_artifacts": [
    {
      "name": "task",
      "description": "架构问答任务规格：topic 描述提问；rubric.required_terms 为答案必须覆盖的术语",
      "kind": "json",
      "content": {
        "topic": "从架构文档回答：OMB v2 记忆存储默认后端与全文检索方案",
        "rubric": { "required_terms": ["SQLite", "FTS5"] }
      }
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "answer": { "type": "string" }
    },
    "required": ["answer"]
  },
  "verifier": {
    "kind": "blind_judge",
    "rules": {
      "rubric": { "required_terms": ["SQLite", "FTS5"] }
    }
  },
  "generator": { "name": "research-03 reference", "version": "1.0.0" }
}
