{
  "id": "research-04",
  "category": "research",
  "requirement": "根据 topic 与 rubric 撰写规范答案并输出 { answer: string }；答案必须覆盖 rubric.required_terms 中的全部术语（离线规则化 judge 判定）。",
  "input_artifacts": [
    {
      "name": "task",
      "description": "风险清单任务规格：topic 描述风险对象；rubric.required_terms 为答案必须覆盖的术语",
      "kind": "json",
      "content": {
        "topic": "列出该技术方案的主要风险",
        "rubric": { "required_terms": ["性能", "安全", "维护"] }
      }
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "answer": { "type": "string" }
    },
    "required": ["answer"]
  },
  "verifier": {
    "kind": "blind_judge",
    "rules": {
      "rubric": { "required_terms": ["性能", "安全", "维护"] }
    }
  },
  "generator": { "name": "research-04 reference", "version": "1.0.0" }
}
