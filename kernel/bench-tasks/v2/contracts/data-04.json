{
  "id": "data-04",
  "category": "data",
  "requirement": "过滤并排序产品列表：保留 price < 50 的产品，按 price 升序（同价按 id 升序），输出 { products: [{ id, price }] }。",
  "input_artifacts": [
    {
      "name": "products",
      "description": "产品列表：每项含 id（字符串）、name（字符串）、price（数字）；含应被过滤的高价项与同价项以检验排序",
      "kind": "json",
      "content": [
        { "id": "p1", "name": "键盘", "price": 19.9 },
        { "id": "p2", "name": "鼠标", "price": 9.9 },
        { "id": "p3", "name": "显示器", "price": 899 },
        { "id": "p4", "name": "笔记本支架", "price": 9.9 },
        { "id": "p5", "name": "降噪耳机", "price": 129 }
      ]
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "products": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "id": { "type": "string" },
            "price": { "type": "number" }
          }
        }
      }
    },
    "required": ["products"]
  },
  "verifier": { "kind": "exact" },
  "generator": { "name": "data-04 reference", "version": "1.0.0" }
}
