{
  "id": "sys-02",
  "category": "sys",
  "requirement": "按变更规格启动服务：输出前后状态断言 { before, after }——before/after 为 键→状态值 映射，状态值取 changes 工件中的字符串 from_status/to_status（键形如 '<service>.status'）。",
  "input_artifacts": [
    {
      "name": "changes",
      "description": "服务启动变更规格：operation=start / service 服务名 / from_status 启动前状态 / to_status 启动后状态",
      "kind": "json",
      "content": {
        "operation": "start",
        "service": "svc",
        "from_status": "stopped",
        "to_status": "running"
      }
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "before": {
        "type": "object",
        "properties": { "svc.status": { "type": "string" } }
      },
      "after": {
        "type": "object",
        "properties": { "svc.status": { "type": "string" } }
      }
    },
    "required": ["before", "after"]
  },
  "verifier": { "kind": "state_assert" },
  "generator": { "name": "sys-02 reference", "version": "1.0.0" }
}
