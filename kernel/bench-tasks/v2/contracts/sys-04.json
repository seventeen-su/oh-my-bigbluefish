{
  "id": "sys-04",
  "category": "sys",
  "requirement": "按变更规格复制目录（源文件清单见 src_files 工件，按 path 有序）：输出前后状态断言 { before, after }——目标目录从 empty 变为包含全部源文件。",
  "input_artifacts": [
    {
      "name": "changes",
      "description": "目录复制变更规格：operation=copy / source 源目录 / target 目标目录",
      "kind": "json",
      "content": {
        "operation": "copy",
        "source": "src",
        "target": "dst"
      }
    },
    {
      "name": "src_files",
      "description": "源目录文件清单（相对路径条目 { path }）；constraints 显式规定扫描根 path / 是否排序 / 是否递归（ChatGPT 意见 4 确定性说明位）",
      "kind": "file-list",
      "constraints": { "path": "src", "sorted": true, "recursive": false },
      "content": [
        { "path": "a.txt" },
        { "path": "b.txt" }
      ]
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "before": { "type": "object" },
      "after": { "type": "object" }
    },
    "required": ["before", "after"]
  },
  "verifier": { "kind": "state_assert" },
  "generator": { "name": "sys-04 reference", "version": "1.0.0" }
}
