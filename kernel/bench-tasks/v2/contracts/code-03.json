{
  "id": "code-03",
  "category": "code",
  "requirement": "用 language 指定的语言实现函数 fizzbuzz(n)：返回 1..n 的字符串数组，3 的倍数替换为 'Fizz'，5 的倍数为 'Buzz'，15 的倍数为 'FizzBuzz'。然后静态推演 test-cases 中的每个用例（输入 → 期望输出），输出 { passed, failed, total } 摘要（passed + failed = total）。",
  "input_artifacts": [
    {
      "name": "test-cases",
      "description": "fizzbuzz 的断言用例：{ name, input, expected }；input 为 n（≥ 1），expected 为字符串数组",
      "kind": "test-cases",
      "content": [
        { "name": "n=1", "input": 1, "expected": ["1"] },
        { "name": "n=3", "input": 3, "expected": ["1", "2", "Fizz"] },
        { "name": "n=5", "input": 5, "expected": ["1", "2", "Fizz", "4", "Buzz"] },
        {
          "name": "n=15",
          "input": 15,
          "expected": ["1", "2", "Fizz", "4", "Buzz", "Fizz", "7", "8", "Fizz", "Buzz", "11", "Fizz", "13", "14", "FizzBuzz"]
        }
      ]
    },
    {
      "name": "language",
      "description": "实现语言",
      "kind": "text",
      "content": "JavaScript"
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "passed": { "type": "number" },
      "failed": { "type": "number" },
      "total": { "type": "number" }
    },
    "required": ["passed", "failed", "total"]
  },
  "verifier": { "kind": "tests" },
  "generator": { "name": "code-03 reference", "version": "1.0.0" }
}
