{
  "id": "code-02",
  "category": "code",
  "requirement": "用 language 指定的语言实现函数 isPalindrome(s)：判断字符串是否为回文（忽略大小写与非字母数字字符）。然后静态推演 test-cases 中的每个用例（输入 → 期望输出），输出 { passed, failed, total } 摘要（passed + failed = total）。",
  "input_artifacts": [
    {
      "name": "test-cases",
      "description": "isPalindrome 的断言用例：{ name, input, expected }；input 为字符串 s，expected 为布尔判定",
      "kind": "test-cases",
      "content": [
        { "name": "回文基础", "input": "racecar", "expected": true },
        { "name": "忽略大小写", "input": "RaceCar", "expected": true },
        { "name": "忽略标点与空格", "input": "A man, a plan, a canal: Panama", "expected": true },
        { "name": "非回文", "input": "hello", "expected": false },
        { "name": "数字串", "input": "12321", "expected": true },
        { "name": "空串", "input": "", "expected": true }
      ]
    },
    {
      "name": "language",
      "description": "实现语言",
      "kind": "text",
      "content": "Python"
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "passed": { "type": "number" },
      "failed": { "type": "number" },
      "total": { "type": "number" }
    },
    "required": ["passed", "failed", "total"]
  },
  "verifier": { "kind": "tests" },
  "generator": { "name": "code-02 reference", "version": "1.0.0" }
}
