{
  "id": "code-01",
  "category": "code",
  "requirement": "用 language 指定的语言实现函数 fib(n)：返回第 n 个斐波那契数（n ≥ 0，fib(0)=0，fib(1)=1；n ≤ 40，禁止递归爆栈）。然后静态推演 test-cases 中的每个用例（输入 → 期望输出），输出 { passed, failed, total } 摘要（passed + failed = total）。",
  "input_artifacts": [
    {
      "name": "test-cases",
      "description": "fib 的断言用例：{ name, input, expected }；input 为 n（≥ 0），expected 为 fib(n)",
      "kind": "test-cases",
      "content": [
        { "name": "fib(0)", "input": 0, "expected": 0 },
        { "name": "fib(1)", "input": 1, "expected": 1 },
        { "name": "fib(2)", "input": 2, "expected": 1 },
        { "name": "fib(10)", "input": 10, "expected": 55 },
        { "name": "fib(20)", "input": 20, "expected": 6765 }
      ]
    },
    {
      "name": "language",
      "description": "实现语言",
      "kind": "text",
      "content": "TypeScript"
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "passed": { "type": "number" },
      "failed": { "type": "number" },
      "total": { "type": "number" }
    },
    "required": ["passed", "failed", "total"]
  },
  "verifier": { "kind": "tests" },
  "generator": { "name": "code-01 reference", "version": "1.0.0" }
}
