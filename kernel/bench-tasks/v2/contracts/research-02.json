{
  "id": "research-02",
  "category": "research",
  "requirement": "根据 topic 与 rubric 撰写规范答案并输出 { answer: string }；答案必须覆盖 rubric.required_terms 中的全部术语（离线规则化 judge 判定）。",
  "input_artifacts": [
    {
      "name": "task",
      "description": "方案比较任务规格：topic 描述比较对象；rubric.required_terms 为答案必须覆盖的术语",
      "kind": "json",
      "content": {
        "topic": "比较方案A 与方案B：必须提及两个方案并给出取舍结论",
        "rubric": { "required_terms": ["方案A", "方案B", "取舍"] }
      }
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "answer": { "type": "string" }
    },
    "required": ["answer"]
  },
  "verifier": {
    "kind": "blind_judge",
    "rules": {
      "rubric": { "required_terms": ["方案A", "方案B", "取舍"] }
    }
  },
  "generator": { "name": "research-02 reference", "version": "1.0.0" }
}
